# Standalone `fastmcp` 3.x, matching Day 3's reference server. Its "http"
# transport answers the SSE GET that Databricks' AI Gateway sends when
# discovering tools; `mcp` 1.x's "streamable-http" returns 406 there.
fastmcp>=3.2.0
requests>=2.32.3
